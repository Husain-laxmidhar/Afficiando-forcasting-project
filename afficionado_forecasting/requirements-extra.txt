# Optional extras for LOCAL development only (Prophet, XGBoost, pmdarima).
# Do NOT deploy these on Streamlit Community Cloud -- prophet and pmdarima
# require compiled build toolchains (cmdstan / Fortran) that commonly fail
# on Cloud's build image and will break the whole deployment.
#
# Install locally with:
#   pip install -r requirements-extra.txt
prophet==1.1.5
xgboost==2.0.3
pmdarima==2.0.4
seaborn>=0.13,<0.14
